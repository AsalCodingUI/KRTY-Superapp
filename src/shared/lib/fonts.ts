// localFont removed since we don't have the font files yet.
// Using standard font stack first to set up the structure.

/*
// Example of how we would set up local fonts if we had them:
import localFont from 'next/font/local'

const switzer = localFont({
  src: [
    {
      path: '../fonts/Switzer-Medium.woff2',
      weight: '500',
      style: 'normal',
    },
    // ... other weights
  ],
  variable: '--font-switzer'
})
*/
